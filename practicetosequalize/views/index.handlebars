<img src="/assets/img/bobeatsbuger.JPG" alt="Burgerimg">
<h1>Burgers!</h1>

<h2>Burgers that have not devoured!</h2>

<ul>
    
    {{#each chores}}
    {{#unless devoured}}
    {{> burgers/burger-block devoured=true}}
    {{/unless}}
    {{/each}}
</ul>

<h2>Burgers that have been devoured!</h2>

<ul>
    {{#each Chores}}
    {{#if chore_done}}
    {{> burgers/burger-block chore_done=false}}
    {{/if}}
    {{/each}}
</ul>

<h2>Add a Chore</h2>

<form class="create-form">
    <div class="form-group">
        <label for="addburg">Chore Name:</label>
        <input type="text" id="addburg" name="name">
    </div>

    <button type="submit"> Submit </button>

</form>